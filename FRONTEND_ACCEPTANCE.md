# YouthLM 最終一次驗收（2026-09-12）

正式專案：`C:\Code\youthlm-test`。這份清單只保留最後一次人工驗收需要做的事；完整限制與歷史在 [BACKEND_TODO.md](BACKEND_TODO.md)。

## 驗收範圍

目前可完整操作：預覽登入與筆記本、全域文字設定、官方來源與多維篩選、白板拖曳連線、右側真實 Assistant 與明確引用、來源分析、成果卡內圖表／資料表與摘要、分析彙整 DOCX 報告與 PPTX 簡報、驗證下載、本機草稿、縮放與政策雷達盤點。

以下仍不是完成品：真實帳號／雲端筆記本、任意檔案上傳與 API 清洗、跨資料集 Join、Assistant 串流與畫布 mutation、簡報逐頁縮圖／線上預覽、文字／畫筆白板註記、政策雷達 AI 建議。預覽密碼不傳送也不保存；不要使用真實密碼。新帳號不會再顯示任何虛構教育、薪資或職訓統計。

## 啟動無金鑰驗收環境

這是完整 UI／HTTP／資料工具／PPTX 整合模式，但摘要文字是測試替身，**不是 AI 政策結論**。

終端一：

```powershell
cd 'C:\Code\youthlm-test'
uv run --frozen python apps/web/test/fixture-api.py
```

終端二：

```powershell
cd 'C:\Code\youthlm-test\apps\web'
$env:VITE_YOUTHLM_API_BASE_URL = 'http://127.0.0.1:18000'
npm run dev -- --host 127.0.0.1 --port 5180 --strictPort
```

開啟 `http://127.0.0.1:5180/`。已有相同服務時不要重複占用 port。

## 最後一次人工檢查（約 10 分鐘）

1. 使用新的測試電子郵件與任意至少 8 字元測試密碼登入。畫面應顯示「建立第一本政策筆記本」，沒有預設假資料。建立並開啟「最終驗收」後，四步操作教學應自動出現；確認可上一步、下一步、略過，並可從頁首「操作教學」重開。兩側面板預設收合，未加入可用資料前不顯示政策雷達。
2. 從空白頁中央按「選擇官方資料」，選「現住人口之年齡分配」，命名「板橋青年人口」。設為 **2022–2024、20–24 歲、合計（官方值）、板橋區**後儲存；畫布不應被放大超過 100%。
3. 從來源卡按「建立圖表」，再按圖表鉛筆開啟設定並執行分析；圖表／表格應依序出現 **28,472、28,174、27,049**，並保留來源、實際篩選、版本及限制。關閉設定後，成果卡本身應直接顯示同一份實際圖表或資料表與摘要；若結果被阻擋或失敗，不得畫出假圖。
4. 從完成圖表卡按「建立簡報」，產生後應顯示封面摘要、「PPTX 已產生」及實際檔案大小。下載並開啟 PPTX，確認文字、圖表、資料表、來源及警告可編輯且不是前端截圖。
5. 新增另一張成果卡，設為「議題研析報告」並選取同一份完成分析；產生後應顯示「DOCX 已產生」及檔案大小。下載並開啟 DOCX，確認封面、摘要、表格、來源、版本、provenance 皆存在且文字／表格可編輯。
6. 開啟右側 AI 小幫手，依序勾選來源、分析與簡報三項明確引用，再詢問「整理這份分析的政策重點、資料限制與下一步」。正式 provider 應產生真實回答，回覆下方應顯示 3 項證據與實際工具執行次數；不得把 fixture 回答視為正式 AI 通過。
7. 點卡片本體或標題不應開啟設定或移動卡片；只有鉛筆會開啟設定，只有右上把手會移動卡片。Ctrl＋滾輪、＋／－、全覽與藍／紫連線應正常。
8. 展開政策雷達並執行盤點，確認來源、圖表、報告及簡報數量合理。返回筆記本列表再重開後，雷達應收合、卡片不被畫布邊緣截切。
9. 重新整理並用同一測試電子郵件登入，筆記本、卡片、位置、設定、連線與已送出的對話應保留；分析／報告／簡報／Assistant 執行狀態需重跑。
10. 信任邊界抽查：改用 **2013、5–9 歲、樹林區**執行人口分析，系統應明確拒絕已知官方異常組合，不可自行補數字。
11. 切換小、中、大、特大文字，確認全站仍可操作且重新整理後保留。開啟 Console，正常流程應沒有紅色應用程式錯誤。

## 自動品質閘門

從 repo 根目錄執行：

```powershell
uv run --frozen python -m app.data_quality
uv run --frozen python -m pytest -q
uv run --frozen ruff check .
cd apps/web
npm run check
npm audit
```

這些檢查涵蓋資料 hash／schema／coverage、Python 契約與 API、前端型別／正式建置／測試，以及相依安全公告。數量以指令當下輸出為準，避免文件數字再次過時。

## 真實 AWS 不是這次本機 UI 驗收

`/health` 200 只代表程序存活；`/ready` 200 只證明 provider 設定存在、封裝資料可用且目前儲存目錄可寫，不能單獨證明 Bedrock IAM 或持久化。必須另讓 event-day Bedrock preflight 全過，並以同一 `/data` volume 重建單 worker／單 replica 容器後取回既有分析與 PPTX，才能宣稱正式流程已就緒。Access Code 只用於 AWS Workshop portal，不是應用密碼或 API 憑證。正式步驟見 [docs/final-environment-runbook.md](docs/final-environment-runbook.md)。
