# YouthLM 最終一次驗收（2026-09-11）

正式專案：`C:\Code\youthlm-test`。這份清單只保留最後一次人工驗收需要做的事；完整限制與歷史在 [BACKEND_TODO.md](BACKEND_TODO.md)。

## 驗收範圍

目前可完整操作：預覽登入與筆記本、全域小／中／大／特大文字設定、官方來源與多維篩選、白板拖曳連線、獨立卡片拖移把手、右側小幫手與需確認的圖表草稿、來源分析、成果卡內圖表／資料表與摘要預覽、分析彙整簡報、簡報卡封面摘要與 PPTX 狀態、PPTX 下載、本機草稿、Ctrl＋滾輪縮放、點空白處收合，以及政策雷達盤點。

以下仍不是完成品：真實帳號／雲端筆記本、任意檔案上傳與 API 清洗、跨資料集 Join、小幫手真實 AI／自由操作、簡報逐頁縮圖／線上預覽、文字／畫筆白板註記、政策雷達 AI 建議。預覽密碼不傳送也不保存；不要使用真實密碼。新帳號不會再顯示任何虛構教育、薪資或職訓統計。

## 啟動無金鑰驗收環境

這是完整 UI／HTTP／資料工具／PPTX 整合模式，但摘要文字是測試替身，**不是 AI 政策結論**。

終端一：

```powershell
cd 'C:\Code\youthlm-test'
uv run --frozen python apps/web/test/fixture-api.py
```

終端二：

```powershell
cd 'C:\Code\youthlm-test\apps\web'
$env:VITE_YOUTHLM_API_BASE_URL = 'http://127.0.0.1:18000'
npm run dev -- --host 127.0.0.1 --port 5180 --strictPort
```

開啟 `http://127.0.0.1:5180/`。已有相同服務時不要重複占用 port。

## 最後一次人工檢查（約 10 分鐘）

1. 使用新的測試電子郵件與任意至少 8 字元測試密碼登入。畫面應顯示「建立第一本政策筆記本」，沒有預設假資料。建立並開啟「最終驗收」後，四步操作教學應自動出現；確認可上一步、下一步、略過，並可從頁首「操作教學」重開。兩側面板預設收合，未加入可用資料前不顯示政策雷達。
2. 從空白頁中央按「選擇官方資料」，選「現住人口之年齡分配」，命名「板橋青年人口」。設為 **2022–2024、20–24 歲、合計（官方值）、板橋區**後儲存；畫布不應被放大超過 100%。
3. 右側 AI 小幫手應顯示 1 個來源。輸入「比較板橋區2022至2024年20至24歲人口趨勢」，送出後應先顯示待確認操作；按「確認並建立到白板」後，系統才在來源右側建立並連接圖表，聊天室保持開啟。它不得假裝已完成 AI 分析。
4. 按圖表鉛筆開啟設定並執行分析；圖表／表格應依序出現 **28,472、28,174、27,049**，並保留來源、實際篩選、版本及限制。關閉設定後，成果卡本身應直接顯示同一份實際圖表（無 visualization 時顯示實際資料表）與摘要；若結果被阻擋或失敗，不得畫出假圖。
5. 從完成圖表卡按「建立簡報」。系統應在右側自動建立並連接簡報且選取該分析；產生後，卡片應直接顯示封面摘要、「PPTX 已產生」及實際檔案大小。再下載並開啟 PPTX，確認文字、圖表、資料表、來源及警告可編輯且不是前端截圖；目前契約沒有逐頁縮圖，不把封面摘要誤認為完整投影片預覽。
6. 點卡片本體或標題不應開啟設定或移動卡片；只有鉛筆編輯按鈕會開啟右側設定，只有卡片右上角拖移把手能移動卡片。編輯後點白板空白處，設定面板應收合；Ctrl＋滾輪、＋／－、全覽、移動後的藍／紫連線都應正常。放開在錯誤端點不得建立關係。
7. 展開政策雷達並執行盤點，確認來源、圖表及簡報數量合理。返回筆記本列表再重開：雷達應自動收合、所有卡片應自動全覽且不被畫布邊緣截切，最近一次雷達紀錄仍在。
8. 重新整理並用同一測試電子郵件登入，筆記本、卡片、位置、設定與連線應保留；分析／簡報執行結果需重跑，不能假裝從本機草稿還原。
9. 信任邊界抽查：改用 **2013、5–9 歲、樹林區**執行人口分析，系統應明確拒絕該已知官方異常組合，不可自行補數字。改回其他年度或年齡後可正常執行。
10. 從頁首「文字大小」依序切換小、中、大、特大，確認登入頁、筆記本、教學、卡片及右側面板同步改變且仍可操作；重新整理後應保留最後選擇。此設定是裝置偏好，不會跟著帳號同步。
11. 開啟瀏覽器開發者工具 Console；正常流程應沒有紅色應用程式錯誤。

## 自動品質閘門

從 repo 根目錄執行：

```powershell
uv run --frozen python -m app.data_quality
uv run --frozen python -m pytest -q
uv run --frozen ruff check .
cd apps/web
npm run check
npm audit
```

這些檢查涵蓋資料 hash／schema／coverage、Python 契約與 API、前端型別／正式建置／測試，以及相依安全公告。數量以指令當下輸出為準，避免文件數字再次過時。

## 真實 AWS 不是這次本機 UI 驗收

`/health` 200 只代表程序存活；`/ready` 200 只證明 provider 設定存在、封裝資料可用且目前儲存目錄可寫，不能單獨證明 Bedrock IAM 或持久化。必須另讓 event-day Bedrock preflight 全過，並以同一 `/data` volume 重建單 worker／單 replica 容器後取回既有分析與 PPTX，才能宣稱正式流程已就緒。Access Code 只用於 AWS Workshop portal，不是應用密碼或 API 憑證。正式步驟見 [docs/final-environment-runbook.md](docs/final-environment-runbook.md)。
